a require script that allows you to use the ;jerk command in infinite yield without risking a ban
## setup
run the following in console or executor: `require(98228461643082).Fun("your_username")` <br />
ok ur done
